class FilePath {
  FilePath();

  static clear(String key) {}

  static fileToPath(dynamic file) {
    assert(false, '不支持客户端');
  }

  static bytesToPath(List<dynamic> fileBits) {
    assert(false, '不支持客户端');
  }

  static pathToFile(String key) {
    assert(false, '不支持客户端');
  }

  static pathIsExist(String key) {
    assert(false, '不支持客户端');
  }
}
